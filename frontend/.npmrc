# ai-elements-nuxt@1.5 declares a peer of nuxt ^3 while the project runs Nuxt
# 4.5, so npm aborts with ERESOLVE unless peer conflicts are tolerated. Keep
# this in the file rather than as a flag on the command line: local installs
# and CI then resolve dependencies the same way.
legacy-peer-deps=true
